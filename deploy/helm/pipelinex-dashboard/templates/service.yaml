apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipelinex-dashboard.fullname" . }}
  labels:
    {{- include "pipelinex-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipelinex-dashboard.selectorLabels" . | nindent 4 }}
